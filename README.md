to build:

have cmake version > 3.24 installed
have c++ compiler installed (GCC, MSVC, etc.)

To configure project, run

	cmake -B build


To build the command line tool, run

	cmake --build build --target cros